import Connections from '@/legacy-pages/Connections';

export const metadata = {
    title: 'Connections | HackMate',
    description: 'View and manage your teammate connections.',
};

export default function ConnectionsPage() {
    return <Connections />;
}
